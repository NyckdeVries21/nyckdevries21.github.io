# Changelog

## Unreleased
- Apply high-tech theme across site (dark default)
- Add CSS variables and multiple palettes (cyberpunk, neon, warm)
- Import `Orbitron` and `Rajdhani` fonts for headings and UI
- Add particles background, typed intro, scroll progress, parallax, tilt cards, reveal-on-scroll
- Add custom cursor and neon ring for profile image
- Add site preloader and palette switcher UI (persisted via localStorage)
- Style PhotoSwipe default skin to match theme
- Small UI polish: button shine, gradients, hover animations

